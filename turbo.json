{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["babel.config.js"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "type-check": {
      "cache": false
    },
    "test": {
      "dependsOn": ["build"]
    }
  }
}
